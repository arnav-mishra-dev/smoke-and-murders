# Supports comma separated values. Do not leave this on *. Fill it out with the URL(s) you use to connect to the frontend. No trailing slashes.
ALLOWED_ORIGINS=*

# The hostname and port the client connects to.
NEXT_PUBLIC_API_HOST=localhost
NEXT_PUBLIC_API_PORT=8080